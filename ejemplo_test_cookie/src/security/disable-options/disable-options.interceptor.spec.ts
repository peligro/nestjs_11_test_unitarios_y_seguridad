import { DisableOptionsInterceptor } from './disable-options.interceptor';

describe('DisableOptionsInterceptor', () => {
  it('should be defined', () => {
    expect(new DisableOptionsInterceptor()).toBeDefined();
  });
});
